% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_lasso.R
\name{plot_lasso}
\alias{plot_lasso}
\title{Plot lasso coefficient paths}
\usage{
plot_lasso(fit, s)
}
\arguments{
\item{fit}{A fitted model from \code{\link[glmnet:glmnet]{glmnet::glmnet()}}.}

\item{s}{Selected value of the penalty parameter.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the coefficient paths of a fitted \code{glmnet} model against the log
lambda sequence and labels the coefficients that remain non-zero at the
selected value of \code{s}.
}
\examples{
\donttest{
if (requireNamespace("glmnet", quietly = TRUE)) {
  set.seed(1)
  x <- matrix(rnorm(100 * 5), nrow = 100)
  y <- rnorm(100) + x[, 1]
  p <- plot_lasso(glmnet::glmnet(x, y), s = 0.05)
}
}
}
