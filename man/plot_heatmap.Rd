% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Heatmap of a matrix}
\usage{
plot_heatmap(
  matrix,
  row_order = NULL,
  column_order = NULL,
  values = FALSE,
  digits = 2,
  midpoint = NULL,
  diverging = FALSE,
  low = NULL,
  mid = NULL,
  high = NULL,
  legend_label = "Value",
  title = NULL,
  font = "Arial"
)
}
\arguments{
\item{matrix}{Numeric matrix, or a data frame whose columns are numeric.}

\item{row_order, column_order}{Optional ordering of rows and columns. Use
\code{"cluster"} to order by hierarchical clustering, \code{"value"} to order by
the first principal component, or supply a character vector of names.}

\item{values}{Print the numeric value in each tile.}

\item{digits}{Significant digits for the printed values.}

\item{midpoint}{Value mapped to the middle of the colourbar. Defaults to the
median, which centres a diverging scale on a typical value.}

\item{diverging}{Use a diverging scale, for values that have a meaningful
centre such as a correlation.}

\item{low, mid, high}{Colours for the low, middle, and high ends.}

\item{legend_label}{Title of the colourbar.}

\item{title}{Plot title.}

\item{font}{Font family used in the plot.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the values of a matrix as tiles with a colourbar. This is the display
for correlation matrices, signature scores by group, and any rectangular
table of values where the pattern matters more than the numbers.
}
\examples{
set.seed(1)
m <- cor(matrix(rnorm(60), nrow = 20))
diag(m) <- NA
p <- plot_heatmap(m, diverging = TRUE, midpoint = 0,
                  legend_label = "Correlation")
}
