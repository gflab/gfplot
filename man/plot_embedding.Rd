% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{plot_embedding}
\alias{plot_embedding}
\title{Grouped embedding scatter}
\usage{
plot_embedding(
  data,
  groups,
  method = c("pca", "tsne", "umap", "none"),
  perplexity = 30,
  seed = NULL,
  ellipse = FALSE,
  label_centres = FALSE,
  palette = "house",
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  font = "Arial"
)
}
\arguments{
\item{data}{A feature matrix with samples in rows, or a two-column matrix of
coordinates when \code{method = "none"}.}

\item{groups}{Group labels, one per sample.}

\item{method}{Embedding to compute: \code{"pca"}, \code{"tsne"}, \code{"umap"}, or
\code{"none"} when \code{data} already holds coordinates.}

\item{perplexity}{Perplexity for t-SNE. Values much larger than the number
of samples are rejected by the back end.}

\item{seed}{Optional seed for the stochastic embeddings.}

\item{ellipse}{Draw a confidence ellipse per group.}

\item{label_centres}{Label each group at the centre of its points.}

\item{palette}{Palette name passed to \code{\link[=get_color]{get_color()}}.}

\item{title, xlab, ylab}{Axis and plot labels.}

\item{font}{Font family used in the plot.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot samples in a two-dimensional embedding, coloured by group. The
embedding can be supplied already computed, or computed here from a feature
matrix with \code{\link[stats:prcomp]{stats::prcomp()}}, \pkg{Rtsne}, or \pkg{umap}.
}
\details{
\code{plot_PCA()} and \code{plot_UMAP()} are thin wrappers around this function, so a
figure keeps the same look whichever embedding it shows.
}
\examples{
set.seed(1)
data <- matrix(rnorm(200), nrow = 20)
groups <- rep(c("A", "B"), each = 10)
p <- plot_embedding(data, groups, method = "pca")
}
\seealso{
\code{\link[=plot_PCA]{plot_PCA()}}, \code{\link[=plot_UMAP]{plot_UMAP()}}
}
