% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{gfplot_colors}
\alias{gfplot_colors}
\title{House colours by semantic role}
\usage{
gfplot_colors(role = "all")
}
\arguments{
\item{role}{Colour roles to return. Use \code{"all"} for every role, or
\code{"series"} for the ordered ramp that multi-series figures walk. Aliases
such as \code{"model_curve"}, \code{"comparator_curve"}, and \code{"reference_line"} map
onto the same roles.}
}
\value{
A named character vector of colours.
}
\description{
Returns the colours \code{gfplot} uses, addressed by role rather than by hex
value. Override any role for a session with
\code{options(gfplot.palette = list(primary = "#123456"))}, which is how the
suite is retuned without editing figures.
}
\examples{
gfplot_colors("primary")
gfplot_colors(c("model_curve", "comparator_curve"))
gfplot_colors("series")
}
\seealso{
\code{\link[=get_color]{get_color()}} for journal palettes and \code{\link[=gfplot_theme]{gfplot_theme()}} for the
type and grid settings that go with these colours.
}
