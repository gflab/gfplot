% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction.R
\name{plot_decision_curve}
\alias{plot_decision_curve}
\title{Decision curve}
\usage{
plot_decision_curve(
  probability,
  outcome,
  positive = NULL,
  thresholds = seq(0.01, 0.5, by = 0.01),
  treat_all = TRUE,
  treat_none = TRUE,
  palette = "house",
  xlab = "Threshold probability",
  ylab = "Net benefit",
  title = NULL,
  font = "Arial"
)
}
\arguments{
\item{probability}{Predicted probabilities, or a matrix or data frame with
one column per model.}

\item{outcome}{Binary outcome, one value per observation.}

\item{positive}{Value of \code{outcome} that represents the event.}

\item{thresholds}{Decision thresholds to evaluate. Defaults to 0.01 to 0.5,
which covers the range used in most clinical decisions.}

\item{treat_all}{Draw the reference curve for treating every patient.}

\item{treat_none}{Draw the reference line for treating no patient.}

\item{palette}{Palette name passed to \code{\link[=get_color]{get_color()}}.}

\item{xlab, ylab, title}{Axis and plot labels.}

\item{font}{Font family used in the plot.}
}
\value{
A \code{ggplot} object.
}
\description{
Net benefit across decision thresholds. Net benefit weights the true
positives a model finds against the false positives it costs, at the
threshold probability a clinician would use to act. It is the figure that
answers whether using the model is better than treating everyone or no one.
}
\examples{
set.seed(1)
outcome <- rbinom(300, 1, 0.3)
probability <- plogis(rnorm(300) + outcome)
p <- plot_decision_curve(probability, outcome)
}
\seealso{
\code{\link[=plot_calibration]{plot_calibration()}} for whether the predicted levels are honest.
}
