% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ROC.R
\name{plot_MulROC}
\alias{plot_MulROC}
\title{Plot several ROC curves from separate datasets}
\usage{
plot_MulROC(
  scores,
  labels,
  palette = "jama_classic",
  color = NULL,
  legend.pos = c(0.4, 0.15),
  title = NULL,
  font = "Arial",
  percent.style = FALSE
)
}
\arguments{
\item{scores}{A named list of numeric marker vectors.}

\item{labels}{A list of binary outcomes, one per element of \code{scores}.}

\item{palette}{Palette name passed to \code{\link[=get_color]{get_color()}}.}

\item{color}{Optional vector of colours; overrides \code{palette}.}

\item{legend.pos}{Legend position as an x/y coordinate pair.}

\item{title}{Plot title.}

\item{font}{Font family used in the plot.}

\item{percent.style}{Label the axes as percentages.}
}
\value{
A \code{ggplot} object.
}
\description{
Each element of \code{scores} is matched with the corresponding element of
\code{labels}, which allows the curves to come from different cohorts or
endpoints rather than from different markers of the same cohort.
}
\examples{
set.seed(1)
scores <- list(cohortA = rnorm(60), cohortB = rnorm(60))
labels <- list(rbinom(60, 1, 0.5), rbinom(60, 1, 0.5))
p <- plot_MulROC(scores, labels)
}
