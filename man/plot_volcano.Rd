% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omics.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Volcano plot}
\usage{
plot_volcano(
  effect,
  p_value,
  label = NULL,
  effect_threshold = 1,
  p_threshold = 0.05,
  label_top = 10,
  palette = "house",
  repel = TRUE,
  xlab = "Effect size",
  ylab = expression(-log[10] ~ italic(P)),
  title = NULL,
  font = "Arial"
)
}
\arguments{
\item{effect}{Numeric effect sizes, typically a log fold change.}

\item{p_value}{Significance values on the same scale as \code{p_threshold}.}

\item{label}{Feature labels.}

\item{effect_threshold, p_threshold}{Calling thresholds. Features past both
are labelled as significant.}

\item{label_top}{Label the most significant features instead of every
significant one. Set to \code{0} to label none, or \code{Inf} to label all.}

\item{palette}{Palette name passed to \code{\link[=get_color]{get_color()}}.}

\item{repel}{Repel the labels so they do not overlap.}

\item{xlab, ylab, title}{Axis and plot labels.}

\item{font}{Font family used in the plot.}
}
\value{
A \code{ggplot} object.
}
\description{
Effect size against significance, with the calling thresholds drawn. This
is the standard first look at a differential analysis: it separates the
features that moved a lot from the ones that moved reliably, and shows how
many of each the thresholds produce.
}
\examples{
set.seed(1)
effect <- rnorm(2000)
p_value <- runif(2000)^2
p <- plot_volcano(effect, p_value, paste0("gene", seq_along(effect)))
}
