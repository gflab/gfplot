% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{gfplot_spec}
\alias{gfplot_spec}
\title{Build a figure specification}
\usage{
gfplot_spec(family, ...)
}
\arguments{
\item{family}{Figure family, for example \code{"forest"}.}

\item{...}{Family-specific arguments. For \code{"forest"}: \code{data}, \code{term},
\code{estimate}, \code{lower}, and \code{upper}, plus the optional settings documented in
\code{\link[=plot_forest]{plot_forest()}}.}
}
\value{
An object of class \code{gfplot_spec}.
}
\description{
Captures the data and the settings for one figure as a value, validates
them, and returns an object that \code{\link[=gfplot_render]{gfplot_render()}} turns into a \code{ggplot}.
Use this when the figure is decided in one place and drawn in another, or
when a specification needs to be stored, compared, or passed across a
process boundary.
}
\details{
Call \code{gfplot_families()} for the available families and their required
arguments.
}
\examples{
effects <- data.frame(
  term = c("Age", "Stage III"),
  hr = c(1.02, 1.84),
  lower = c(0.99, 1.51),
  upper = c(1.05, 2.24)
)
spec <- gfplot_spec(
  "forest",
  data = effects, term = "term",
  estimate = "hr", lower = "lower", upper = "upper"
)
spec
}
\seealso{
\code{\link[=gfplot_render]{gfplot_render()}}, \code{\link[=plot_forest]{plot_forest()}}
}
