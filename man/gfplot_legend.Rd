% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{gfplot_legend}
\alias{gfplot_legend}
\title{Legend layout for a set of series labels}
\usage{
gfplot_legend(labels, base_size = 11, hide_single = TRUE)
}
\arguments{
\item{labels}{Series labels the legend will show.}

\item{base_size}{Base font size, used to scale the legend keys.}

\item{hide_single}{Hide the legend when only one series is present, where it
would only restate the axis. Set to \code{FALSE} when the legend carries
information, as with the area under the curve printed by \code{\link[=plot_ROC]{plot_ROC()}}.}
}
\value{
A \code{ggplot2} guide specification, or \code{"none"} for a single series
when \code{hide_single = TRUE}.
}
\description{
Legends in \code{gfplot} figures sit under the panel and wrap onto as many rows
as the number of series needs, so a wide legend does not squeeze the panel.
}
\examples{
gfplot_legend(c("Low risk", "High risk"))
}
