% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omics.R
\name{plot_waterfall}
\alias{plot_waterfall}
\title{Waterfall plot of response}
\usage{
plot_waterfall(
  response,
  category = NULL,
  patient = NULL,
  label_top = 0,
  thresholds = c(`Partial response` = -30, `Stable disease` = 20),
  threshold_labels = TRUE,
  show_patients = FALSE,
  palette = "house",
  xlab = NULL,
  ylab = "Change from baseline (\%)",
  title = NULL,
  font = "Arial"
)
}
\arguments{
\item{response}{Numeric response values, for example a percentage change
from baseline. \code{NA} values are dropped.}

\item{category}{Optional response category per patient, used to fill the
bars. Defaults to the RECIST-style cut-offs, where -30 and 20 percent
separate partial response, stable disease, and progression.}

\item{patient}{Optional patient labels, used on the x axis when few enough
bars are drawn to read them.}

\item{label_top}{Label this many of the largest and smallest bars, which is
what a reader wants from a dense waterfall.}

\item{thresholds}{Cut-offs between response categories, as a named vector
such as \code{c("Partial response" = -30, "Stable disease" = 20)}. Values at
or below the first are partial response and values above the last are
progression.}

\item{threshold_labels}{Print the cut-off lines and their values.}

\item{show_patients}{Print patient labels under the bars.}

\item{palette}{Palette name passed to \code{\link[=get_color]{get_color()}}.}

\item{xlab, ylab, title}{Axis and plot labels.}

\item{font}{Font family used in the plot.}
}
\value{
A \code{ggplot} object.
}
\description{
Patients ranked by response, with the bar height showing how far each
departed from baseline and the fill showing the response category. This is
the standard display of response in a single-arm trial.
}
\examples{
set.seed(1)
response <- c(rnorm(40, -10, 20), rnorm(10, 40, 15))
p <- plot_waterfall(response)
}
