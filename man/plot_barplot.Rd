% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_stat_figures.R
\name{plot_barplot}
\alias{plot_barplot}
\title{Grouped bar plot with optional significance annotations}
\usage{
plot_barplot(
  value,
  group,
  comparisons = NULL,
  palette = "house",
  color = NULL,
  ylab = "Score",
  xlab = NULL,
  title = NULL,
  font = "Arial",
  label = "p.signif"
)
}
\arguments{
\item{value}{Numeric values to summarise.}

\item{group}{Grouping vector, one value per observation.}

\item{comparisons}{Optional list of length-two vectors naming the groups to
compare, for example \code{list(c("A", "B"))}.}

\item{palette}{Palette name passed to \code{\link[=get_color]{get_color()}}.}

\item{color}{Optional vector of fill colours; overrides \code{palette}.}

\item{ylab, xlab, title}{Axis and plot labels.}

\item{font}{Font family used in the plot.}

\item{label}{Significance label style passed to
\code{\link[ggpubr:stat_compare_means]{ggpubr::stat_compare_means()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the mean of \code{value} per group with standard error bars, and adds
pairwise significance labels when \code{comparisons} is supplied.
}
\details{
This function replaced the 0.1.0 \code{plot_barplot()}, which took no arguments
and could not be called.
}
\examples{
set.seed(1)
p <- plot_barplot(
  value = c(rnorm(20), rnorm(20, 1)),
  group = rep(c("A", "B"), each = 20)
)

if (requireNamespace("ggpubr", quietly = TRUE)) {
  p <- plot_barplot(
    value = c(rnorm(20), rnorm(20, 1), rnorm(20, 2)),
    group = rep(c("A", "B", "C"), each = 20),
    comparisons = list(c("A", "B"), c("B", "C"))
  )
}
}
