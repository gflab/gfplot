% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction.R
\name{plot_pr_curve}
\alias{plot_pr_curve}
\title{Precision-recall curve}
\usage{
plot_pr_curve(
  scores,
  labels,
  positive = NULL,
  palette = "house",
  title = NULL,
  font = "Arial",
  show_auc = TRUE
)
}
\arguments{
\item{scores}{Numeric vector, or a matrix or data frame with one column per
marker.}

\item{labels}{Binary outcome, one value per observation.}

\item{positive}{Percentile of the scores above which the positive class is
expected. Used only to draw the no-skill reference line, which is the
prevalence of the positive class.}

\item{palette}{Palette name passed to \code{\link[=get_color]{get_color()}}.}

\item{title}{Plot title.}

\item{font}{Font family used in the plot.}

\item{show_auc}{Print the area under the curve, computed by the trapezoid
rule, in the legend.}
}
\value{
A \code{ggplot} object.
}
\description{
Precision against recall for a binary outcome. On an imbalanced outcome the
ROC curve can look encouraging while precision stays low, which is what
this plot exposes: it is the recommended companion when the event is rare.
}
\examples{
set.seed(1)
outcome <- rbinom(200, 1, 0.15)
score <- outcome * 0.8 + rnorm(200)
p <- plot_pr_curve(score, outcome)
}
\seealso{
\code{\link[=plot_ROC]{plot_ROC()}} for discrimination, \code{\link[=plot_calibration]{plot_calibration()}} for the level
of the predictions.
}
