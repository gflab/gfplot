% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix.R
\name{plot_confusion}
\alias{plot_confusion}
\title{Confusion matrix}
\usage{
plot_confusion(
  predicted = NULL,
  actual = NULL,
  predicted_labels = NULL,
  actual_labels = NULL,
  positive = NULL,
  show = c("count", "percent", "both"),
  digits = 1,
  title = NULL,
  font = "Arial"
)
}
\arguments{
\item{predicted, predicted_labels}{Predicted class labels.}

\item{actual, actual_labels}{Actual class labels. \code{actual} may also be a
table or matrix of counts, in which case the labels are taken from its
dimnames.}

\item{positive}{Value of the outcome that counts as the positive class,
used to order and label the axes. With \code{positive} supplied, the two
classes are shown as \code{"Negative"} and \code{"Positive"}; pass
\code{predicted_labels} and \code{actual_labels} to name them differently.}

\item{show}{Which numbers to print: \code{"count"}, \code{"percent"} (by actual
class), or \code{"both"}.}

\item{digits}{Significant digits for percentages.}

\item{title}{Plot title.}

\item{font}{Font family used in the plot.}
}
\value{
A \code{ggplot} object.
}
\description{
Predicted against actual class, with the count in each cell. Percentages
are given per actual class, which is the direction that shows sensitivity
and specificity; overall percentages would hide a class the model misses.
}
\examples{
set.seed(1)
actual <- rbinom(200, 1, 0.4)
predicted <- ifelse(runif(200) < 0.8, actual, 1 - actual)
p <- plot_confusion(predicted, actual, positive = 1)
}
