% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction.R
\name{plot_calibration}
\alias{plot_calibration}
\title{Calibration curve}
\usage{
plot_calibration(
  probability,
  outcome,
  positive = NULL,
  bins = 10,
  smooth = FALSE,
  show_ci = TRUE,
  palette = "house",
  ci_level = 0.95,
  xlab = "Predicted probability",
  ylab = "Observed proportion",
  title = NULL,
  font = "Arial"
)
}
\arguments{
\item{probability}{Predicted probabilities, or a matrix or data frame with
one column per model.}

\item{outcome}{Binary outcome, one value per observation.}

\item{positive}{Value of \code{outcome} that represents the event.}

\item{bins}{Number of groups to divide the predictions into. Quantile bins
are used, so every point rests on a similar number of observations.}

\item{smooth}{Draw a loess curve through the points instead of connecting
them.}

\item{show_ci}{Draw binomial confidence intervals for each point.}

\item{palette}{Palette name passed to \code{\link[=get_color]{get_color()}}.}

\item{ci_level}{Confidence level of the intervals.}

\item{xlab, ylab, title}{Axis and plot labels.}

\item{font}{Font family used in the plot.}
}
\value{
A \code{ggplot} object.
}
\description{
Predicted probability against the observed proportion of events. A model
whose points sit on the diagonal predicts the level it claims; one above
the diagonal under-predicts risk and one below over-predicts.
}
\examples{
set.seed(1)
outcome <- rbinom(200, 1, 0.4)
probability <- plogis(rnorm(200) + outcome * 0.5)
p <- plot_calibration(probability, outcome)
}
\seealso{
\code{\link[=plot_ROC]{plot_ROC()}} for discrimination, \code{\link[=plot_decision_curve]{plot_decision_curve()}} for
whether acting on the predictions helps.
}
