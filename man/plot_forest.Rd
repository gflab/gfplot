% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest.R
\name{plot_forest}
\alias{plot_forest}
\title{Forest plot of effect estimates}
\usage{
plot_forest(
  data,
  term,
  estimate,
  lower,
  upper,
  p = NULL,
  group = NULL,
  label = NULL,
  log_scale = FALSE,
  ref_line = NULL,
  estimate_label = "Estimate (95\% CI)",
  p_label = "P",
  show_table = TRUE,
  digits = 2,
  xlab = NULL,
  title = NULL,
  font = "Arial"
)
}
\arguments{
\item{data}{Data frame holding the estimates.}

\item{term}{Column of row labels, one per estimate.}

\item{estimate}{Column of point estimates.}

\item{lower, upper}{Columns of the confidence limits.}

\item{p}{Optional column of p-values, shown as a right-hand column.}

\item{group}{Optional column naming the section each estimate belongs to.
Sections are drawn as facets with a shared axis.}

\item{label}{Optional column overriding \code{term} for display, for example a
formatted "Age, per year" instead of a variable name.}

\item{log_scale}{Plot the axis on a log scale, which is what ratios need so
that a halving and a doubling are the same distance.}

\item{ref_line}{Value of the reference line. Defaults to 1 when
\code{log_scale = TRUE} and 0 otherwise.}

\item{estimate_label}{Column heading for the estimate table.}

\item{p_label}{Column heading for the p-value column.}

\item{show_table}{Print the estimate and p-value columns.}

\item{digits}{Significant digits for the printed numbers.}

\item{xlab, title}{Axis and plot labels.}

\item{font}{Font family used in the plot.}
}
\value{
A \code{ggplot} object, or a composed panel when \code{show_table = TRUE},
as with the risk table of \code{\link[=plot_KMCurve]{plot_KMCurve()}}. Both print and save the same
way.
}
\description{
Draws point estimates with confidence intervals on a horizontal axis, with
the numbers printed alongside. This is the standard display for hazard
ratios, odds ratios, and mean differences, and it reads a table straight
from \code{clinstats::cox_table()} or \code{clinstats::logistic_table()}.
}
\examples{
effects <- data.frame(
  term = c("Age, per year", "Stage III vs II", "MSI-high"),
  hr = c(1.02, 1.84, 0.62),
  lower = c(0.99, 1.51, 0.44),
  upper = c(1.05, 2.24, 0.88),
  p = c(0.21, 4.1e-07, 0.008)
)
p <- plot_forest(
  effects,
  term = "term", estimate = "hr", lower = "lower", upper = "upper",
  p = "p", log_scale = TRUE
)

# Straight from a clinstats table
\donttest{
if (requireNamespace("clinstats", quietly = TRUE)) {
  tbl <- clinstats::cox_table(
    clinstats::clin_crc,
    time = "rfs.delay", event = "rfs.event",
    factors = c("sex", "age", "tnm.stage"), multivariable = "none"
  )
  p <- plot_forest(
    tbl,
    term = "term", estimate = "hr_univariable",
    lower = "univ_ci_lower", upper = "univ_ci_upper", p = "univ_p",
    log_scale = TRUE
  )
}
}
}
\seealso{
\code{\link[=gfplot_spec]{gfplot_spec()}} to build the same figure as a value. The companion
package \code{clinstats} produces the estimates this draws.
}
