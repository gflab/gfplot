% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ROC.R
\name{plot_TimeROC}
\alias{plot_TimeROC}
\title{Plot time-dependent ROC curves}
\usage{
plot_TimeROC(
  scores,
  survival,
  time_points,
  groups,
  palette = "jama",
  legend.pos = c(0.4, 0.15),
  title = NULL,
  font = "Arial",
  percent.style = FALSE
)
}
\arguments{
\item{scores}{Numeric marker values.}

\item{survival}{A two-column survival object.}

\item{time_points}{Time points at which the curves are evaluated.}

\item{groups}{Labels for the curves, one per time point.}

\item{palette}{Palette name passed to \code{\link[=get_color]{get_color()}}.}

\item{legend.pos}{Legend position as an x/y coordinate pair.}

\item{title}{Plot title.}

\item{font}{Font family used in the plot.}

\item{percent.style}{Label the axes as percentages.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots survival ROC curves at one or more time points using the
Kaplan-Meier method of the \code{survivalROC} package.
}
\examples{
\donttest{
if (requireNamespace("survivalROC", quietly = TRUE)) {
  set.seed(1)
  surv <- survival::Surv(rexp(100), rbinom(100, 1, 0.6))
  p <- plot_TimeROC(rnorm(100), surv, c(1, 2), c("year1", "year2"))
}
}
}
