% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_stat_figures.R
\name{plot_tsne}
\alias{plot_tsne}
\title{t-SNE projection of samples coloured by group}
\usage{
plot_tsne(
  data,
  labs,
  title = "Evaluate the batch effect between groups",
  perplexity = 30,
  seed = 1,
  palette = "house",
  font = "Arial",
  ...
)
}
\arguments{
\item{data}{Matrix or data frame with samples in rows and features in
columns.}

\item{labs}{Group labels, one per sample.}

\item{title}{Plot title.}

\item{perplexity}{Perplexity for the t-SNE embedding.}

\item{seed}{Optional seed; t-SNE is stochastic, so a seed makes a figure
reproducible.}

\item{palette}{Palette name passed to \code{\link[=get_color]{get_color()}}.}

\item{font}{Font family used in the plot.}

\item{...}{Passed to \code{\link[=plot_embedding]{plot_embedding()}}, for example \code{ellipse}, \code{seed}, or
\code{label_centres}.}
}
\value{
A \code{ggplot} object.
}
\description{
t-SNE projection of samples coloured by group
}
\examples{
\donttest{
if (requireNamespace("Rtsne", quietly = TRUE)) {
  set.seed(1)
  p <- plot_tsne(matrix(rnorm(200), nrow = 20), rep(c("A", "B"), each = 10))
}
}
}
