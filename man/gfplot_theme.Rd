% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style.R
\name{gfplot_theme}
\alias{gfplot_theme}
\title{House theme for publication figures}
\usage{
gfplot_theme(
  base_size = 11,
  font = "Arial",
  grid = TRUE,
  border = TRUE,
  legend = c("bottom", "right", "top", "left", "none"),
  margin = c(9, 10, 8, 10)
)
}
\arguments{
\item{base_size}{Base font size in points. Other sizes are derived from it.}

\item{font}{Font family. Defaults to Arial.}

\item{grid}{Draw the major grid.}

\item{border}{Draw the panel border.}

\item{legend}{Legend position, one of \code{"bottom"}, \code{"right"}, \code{"top"},
\code{"left"}, or \code{"none"}.}

\item{margin}{Plot margin in points, in the order top, right, bottom, left.}
}
\value{
A \code{ggplot2} theme object.
}
\description{
The theme every \code{gfplot} figure is drawn with: bold axis titles, a thin
major grid, a panel border, and a horizontal legend under the panel. Sizes
derive from \code{base_size}, so one number rescales the figure.
}
\details{
This theme is also the session baseline: attaching \code{gfplot} sets it as the
global \code{ggplot2} theme so that figures drawn by other packages match. Set
\code{options(gfplot.set_theme = FALSE)} before \code{library(gfplot)} to keep an
existing theme instead.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  gfplot_theme()
}
\seealso{
\code{\link[=gfplot_colors]{gfplot_colors()}} for the colours that go with it.
}
